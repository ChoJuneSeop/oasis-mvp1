# Validation records

`EXPERIMENT_READY.json` is generated by the full preflight. If any gate fails, its
status is `ADMISSION_FAIL` and the failed evidence remains recorded. Rules are not
changed after observing a failed or successful gate.
