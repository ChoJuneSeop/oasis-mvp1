# Governance OASIS A6 Wrong-Change Recovery v1

A6 closes the full recovery chain that GH-3 and CBRA v1.1 only partially covered.

The decision worker never receives a wrongness label, future outcome, evaluator
truth, or the post-outcome adverse classification.

Temporal chain:

1. prior Completed Experience exists;
2. CE exposed/hidden contrast produces an actually realized initial action;
3. only after realization an authoritative observation is created;
4. independent evaluator applies the preregistered adverse rule after all initial workers are sealed;
5. Closure and provenance-bound revalidation commit;
6. a later decision actually realizes either recovery or recurrence.

Five arms:

- INITIAL_EXPERIENCE_EXPOSED
- INITIAL_EXPERIENCE_HIDDEN
- REVALIDATION_EXPOSED
- REVALIDATION_RECORD_ONLY
- EXOGENOUS_ATTRIBUTION_CONTROL

SAME_SCOPE and UNRELATED_RELATION are both required. The unrelated relation is
independently CE-eligible, so noninheritance cannot pass trivially.

Pilot P is structural only. Confirmatory F1/F2/F3 contains 30 chains and 60
actual decision-realization epochs.

Claim boundary: finite deterministic synthetic recovery mechanism only.
