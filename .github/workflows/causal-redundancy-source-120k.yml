name: OASIS CR-03R Redundancy Source 120k

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - "tools/causal-redundancy-source-120k.mjs"
      - "tools/validation-compatibility-guard.mjs"
      - ".github/workflows/causal-redundancy-source-120k.yml"

concurrency:
  group: oasis-cr03r-redundancy-source-120k
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install Playwright
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - name: Guard canonical validation conditions
        run: node tools/validation-compatibility-guard.mjs
      - name: Run CR-03R redundancy source decomposition
        run: node tools/causal-redundancy-source-120k.mjs
      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: oasis-cr03r-redundancy-source-120k
          path: causal-redundancy-source-120k-report.json
          if-no-files-found: ignore
