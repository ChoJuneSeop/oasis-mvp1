name: OASIS EX-04P Longitudinal Disposition Lineage v3

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - "tools/ex04p-longitudinal-relational-disposition-lineage-v3.mjs"
      - "relation-field.js"
      - "index.html"
      - "tools/validation-compatibility-guard.mjs"
      - ".github/workflows/ex04p-longitudinal-relational-disposition-lineage-v3.yml"

concurrency:
  group: oasis-ex04p-longitudinal-disposition-lineage-v3
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install Playwright
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - name: Guard canonical validation conditions
        run: node tools/validation-compatibility-guard.mjs
      - name: Run EX-04P longitudinal disposition lineage replay
        run: node tools/ex04p-longitudinal-relational-disposition-lineage-v3.mjs
      - name: Upload EX-04P disposition lineage evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: oasis-ex04p-longitudinal-disposition-lineage-v3
          path: ex04p-longitudinal-relational-disposition-lineage-v3-report.json
          if-no-files-found: ignore
