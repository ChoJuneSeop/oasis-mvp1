name: OASIS Exact Provenance Bridge Pilot

on:
  push:
    paths:
      - 'tools/latent-relation-provenance-bridge-validation.mjs'
      - '.github/workflows/latent-relation-provenance-bridge.yml'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install Playwright
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - name: Guard canonical validation conditions
        run: node tools/validation-compatibility-guard.mjs
      - name: Run 30k exact provenance bridge pilot
        env:
          OASIS_PROVENANCE_BRIDGE_MAX_TICK: 30000
        run: node tools/latent-relation-provenance-bridge-validation.mjs
      - name: Upload provenance bridge audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: oasis-latent-relation-provenance-bridge-pilot
          path: |
            latent-relation-provenance-bridge-report.json
            latent-relation-provenance-bridge-audit.jsonl
            latent-relation-provenance-bridge-counterfactual.jsonl
          if-no-files-found: ignore
