name: OASIS Budget-Matched Periodic Null Control

on:
  push:
    paths:
      - 'tools/latent-relation-budget-periodic-control.mjs'
      - '.github/workflows/latent-relation-budget-periodic-control.yml'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install Playwright
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - name: Guard canonical validation conditions
        run: node tools/validation-compatibility-guard.mjs
      - name: Run 30k budget-matched periodic null controls
        run: node tools/latent-relation-budget-periodic-control.mjs
      - name: Upload budget-periodic report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: oasis-latent-budget-periodic-control
          path: latent-relation-budget-periodic-control-report.json
          if-no-files-found: ignore
