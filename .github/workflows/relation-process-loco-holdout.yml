name: OASIS Relation Process LOCO Holdout

on:
  push:
    branches: ["validation/relation-process-loco-holdout"]
    paths:
      - "tools/relation-process-loco-composition-holdout-test.mjs"
      - ".github/workflows/relation-process-loco-holdout.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  loco-holdout:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Run frozen LOCO holdout
        run: node tools/relation-process-loco-composition-holdout-test.mjs
      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: relation-process-loco-composition-holdout-report
          path: relation-process-loco-composition-holdout-report.json
          if-no-files-found: ignore
