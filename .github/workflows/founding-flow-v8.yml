name: Founding Flow v8

on:
  push:
    branches:
      - architecture/oasis-unified-validation-system-v1
    paths:
      - 'experiments/founding-flow-v8/**'
      - 'src/validation/founding-flow-v7-ancestors.mjs'
      - 'src/validation/founding-flow-v5-ancestors.mjs'
      - 'src/oasis-core.mjs'
      - 'src/oasis-integrated-core.mjs'
      - 'src/oasis-reference-core.mjs'
      - 'tools/founding-flow-v8.mjs'
      - '.github/workflows/founding-flow-v8.yml'

jobs:
  founding-flow-v8:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Syntax check
        run: |
          node --check src/validation/founding-flow-v7-ancestors.mjs
          node --check tools/founding-flow-v8.mjs
      - name: Unified-system contamination boundary audit
        run: node tools/oasis-unified-validation-system-audit.mjs
      - name: Execute C6 necessity audit
        run: |
          mkdir -p artifacts
          node tools/founding-flow-v8.mjs | tee artifacts/founding-flow-v8.log
      - name: Upload audit artifact
        uses: actions/upload-artifact@v4
        with:
          name: founding-flow-v8-report
          path: |
            artifacts/founding-flow-v8.json
            artifacts/founding-flow-v8.log
          if-no-files-found: error
