import { writeFile } from 'node:fs/promises';

// Fresh paired holdout: no seed from the prior LOCO or matched-transition holdouts is reused.
const TRAIN_SEEDS=[307,661,991,1429,1871,2357,2909,3473,4051,4639,5261,5903];
const HOLDOUT_SEEDS=[51013,51521,52067,52609,53147,53681,54217,54767,55313,55871,56417,56963];
const LEVELS=[-1,0,1], ACTIONS=LEVELS.flatMap(a=>LEVELS.map(b=>[a,b]));
const MEMORY_PER_SEED=40, QUERIES_PER_SEED=10, BASE_K=36, EXTRA_K=12, RIDGE=.08, EPS=1e-9;
const SCALES=[.5,1,2], MIN_PAIR_SUPPORT=2, MAX_ANCHORS=14, PAIR_CALIPER_FACTOR=.90, OTHER_MISMATCH_PENALTY=.55;
const clamp=(x,a,b)=>Math.max(a,Math.min(b,x));
const mean=xs=>xs.length?xs.reduce((a,b)=>a+b,0)/xs.length:null;
const median=xs=>{if(!xs.length)return null;const a=[...xs].sort((x,y)=>x-y),m=Math.floor(a.length/2);return a.length%2?a[m]:(a[m-1]+a[m])/2};
const dot=(a,b)=>a.reduce((s,x,i)=>s+x*b[i],0),norm=a=>Math.hypot(...a),mul=(a,s)=>a.map(x=>x*s),add=(a,b)=>a.map((x,i)=>x+b[i]);
const rot=(v,a)=>[Math.cos(a)*v[0]-Math.sin(a)*v[1],Math.sin(a)*v[0]+Math.cos(a)*v[1]],ang=v=>Math.atan2(v[1],v[0]);
const wrap=a=>{while(a>Math.PI)a-=2*Math.PI;while(a<-Math.PI)a+=2*Math.PI;return a},key=a=>`${a[0]},${a[1]}`;
function hash32(...xs){let h=2166136261>>>0,s=xs.join('|');for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)>>>0;}h^=h>>>16;h=Math.imul(h,0x7feb352d)>>>0;h^=h>>>15;h=Math.imul(h,0x846ca68b)>>>0;h^=h>>>16;return h>>>0}
const rand=(...xs)=>hash32(...xs)/4294967296;
function context(seed,i,kind){const th=2*Math.PI*rand('th',seed,i,kind),drift=2*rand('drift',seed,i,kind)-1,swirl=2*rand('swirl',seed,i,kind)-1,asym=2*rand('asym',seed,i,kind)-1,mag=.045+.055*rand('mag',seed,i,kind),before=mul([Math.cos(th),Math.sin(th)],mag),process=[Math.cos(th),Math.sin(th),drift,swirl,asym,mag/.10],baseGain=1.035+.075*(.5+.5*Math.sin(1.17*th+.61*drift-.37*swirl))+.018*Math.abs(asym),commonFlowLoad=clamp((baseGain-1)/.13,0,1);return{th,drift,swirl,asym,mag,before,process,baseGain,commonFlowLoad}}
function dynamics(ctx,action,tag){const[a1,a2]=action,b1=.076*Math.tanh(1.18*ctx.process[0]+.72*ctx.drift-.43*ctx.swirl+.22*ctx.asym),b2=.073*Math.tanh(-.82*ctx.process[1]+.31*ctx.drift+1.04*ctx.swirl-.28*ctx.asym),synergy=.034*a1*a2*Math.sin(1.61*ctx.th+.77*ctx.asym-.34*ctx.drift),cost=.011*(a1*a1+a2*a2),gain=clamp(ctx.baseGain-a1*b1-a2*b2-synergy+cost,.74,1.24),turn=.19*ctx.swirl+.105*a1-.092*a2+.074*a1*a2*ctx.asym,det=mul(rot(ctx.before,turn),gain),nm=.0018,after=add(det,[(rand('nx',tag)-.5)*2*nm,(rand('ny',tag)-.5)*2*nm]),logGain=Math.log(Math.max(EPS,norm(after)/(norm(ctx.before)+EPS))),observedTurn=wrap(ang(after)-ang(ctx.before));return{gain,after,logGain,resolution:-logGain,observedTurn}}
function makeMemory(){const out=[];for(const s of TRAIN_SEEDS)for(let i=0;i<MEMORY_PER_SEED;i++){const ctx=context(s,i,'memory'),action=ACTIONS[Math.floor(rand('action',s,i)*ACTIONS.length)],d=dynamics(ctx,action,`m:${s}:${i}:${key(action)}`);out.push({ctx,action,...d})}return out}
function dist(a,b){const w=[1,1,.72,.72,.55,.45];return Math.sqrt(a.reduce((s,x,i)=>s+w[i]*(x-b[i])**2,0))}
function activate(memory,ctx,scale){const base=BASE_K+Math.round(EXTRA_K*ctx.commonFlowLoad),k=Math.max(12,Math.min(memory.length,Math.round(base*scale))),rows=memory.map(r=>({...r,d:dist(r.ctx.process,ctx.process)})).sort((a,b)=>a.d-b.d).slice(0,k),local=median(rows.map(r=>r.d))||1;for(const r of rows)r.w=Math.exp(-r.d/(local+EPS));return rows}
function wmean(rows,fn){let s=0,w=0;for(const r of rows){s+=r.w*fn(r);w+=r.w}return w?s/w:null}
function solve(A,b,lambda=RIDGE){const n=A[0].length,M=Array.from({length:n},(_,i)=>Array.from({length:n+1},(_,j)=>j<n?(A.reduce((s,row)=>s+row[i]*row[j],0)+(i===j?lambda:0)):A.reduce((s,row,k)=>s+row[i]*b[k],0)));for(let c=0;c<n;c++){let p=c;for(let r=c+1;r<n;r++)if(Math.abs(M[r][c])>Math.abs(M[p][c]))p=r;[M[c],M[p]]=[M[p],M[c]];const d=M[c][c];if(Math.abs(d)<1e-10)continue;for(let j=c;j<=n;j++)M[c][j]/=d;for(let r=0;r<n;r++){if(r===c)continue;const f=M[r][c];for(let j=c;j<=n;j++)M[r][j]-=f*M[c][j]}}return M.map(r=>r[n])}
function feat(ctx,a){const[x,y]=a,p=ctx.process;return[1,x,y,x*y,x*p[0],x*p[2],x*p[4],y*p[1],y*p[3],y*p[4],x*y*p[2],x*y*p[4]]}
function localRewardRidge(active,ctx){if(active.length<12)return[0,0];const A=[],b=[];for(const r of active){const f=feat(r.ctx,r.action),s=Math.sqrt(r.w);A.push(f.map(x=>x*s));b.push(r.resolution*s)}const beta=solve(A,b);let best=ACTIONS[0],bv=-Infinity;for(const a of ACTIONS){const v=dot(feat(ctx,a),beta);if(v>bv){bv=v;best=a}}return best}
function relationProcess(active,ctx){const base=wmean(active,r=>r.logGain)??0,comp=[new Map(),new Map()];for(let d=0;d<2;d++)for(const l of LEVELS){const rows=active.filter(r=>r.action[d]===l);comp[d].set(l,rows.length?(wmean(rows,r=>r.logGain)??base)-base:0)}const pair=new Map;for(const a of ACTIONS){const rows=active.filter(r=>key(r.action)===key(a));if(rows.length>=2){const m=wmean(rows,r=>r.logGain)??base,additive=base+(comp[0].get(a[0])||0)+(comp[1].get(a[1])||0),rel=Math.min(.55,rows.length/8);pair.set(key(a),(m-additive)*rel)}else pair.set(key(a),0)}const ca=ang(ctx.before),orient=[new Map(),new Map()];for(let d=0;d<2;d++)for(const l of LEVELS){const rows=active.filter(r=>r.action[d]===l);orient[d].set(l,rows.length?(wmean(rows,r=>Math.cos(wrap(ang(r.ctx.before)-ca))*Math.max(-.2,Math.min(.2,r.resolution)))??0):0)}let best=ACTIONS[0],bv=Infinity;for(const a of ACTIONS){let pred=base+(comp[0].get(a[0])||0)+(comp[1].get(a[1])||0)+(pair.get(key(a))||0);pred-=.10*((orient[0].get(a[0])||0)+(orient[1].get(a[1])||0));if(pred<bv){bv=pred;best=a}}return best}
function matchedPairs(active,dim,from,to,otherLevel,ctx){if(from===to)return[];const left=active.filter(r=>r.action[dim]===from&&r.action[1-dim]===otherLevel),right=active.filter(r=>r.action[dim]===to&&r.action[1-dim]===otherLevel);if(!left.length||!right.length)return[];const localScale=median(active.map(r=>r.d))||1,caliper=PAIR_CALIPER_FACTOR*localScale,pairs=[];for(const a of left){let b=null,bd=Infinity;for(const x of right){const z=dist(a.ctx.process,x.ctx.process);if(z<bd){bd=z;b=x}}if(!b||bd>caliper)continue;const mid=a.ctx.process.map((x,i)=>(x+b.ctx.process[i])/2),queryDist=dist(mid,ctx.process),w=Math.sqrt(a.w*b.w)*Math.exp(-bd/(localScale+EPS))*Math.exp(-queryDist/(localScale+EPS));pairs.push({a,b,pairDist:bd,w,deltaLog:b.logGain-a.logGain,deltaTurn:wrap(b.observedTurn-a.observedTurn)})}return pairs}
function stepEffect(active,ctx,dim,from,to,otherLevel){if(from===to)return{ok:true,deltaLog:0,deltaTurn:0,support:999,weight:1,pairDist:0};let rows=matchedPairs(active,dim,from,to,otherLevel,ctx);if(rows.length<MIN_PAIR_SUPPORT){const alt=[];for(const o of LEVELS){if(o===otherLevel)continue;for(const p of matchedPairs(active,dim,from,to,o,ctx))alt.push({...p,w:p.w*OTHER_MISMATCH_PENALTY})}rows=rows.concat(alt)}if(rows.length<MIN_PAIR_SUPPORT)return{ok:false,deltaLog:0,deltaTurn:0,support:rows.length,weight:0,pairDist:null};rows.sort((x,y)=>y.w-x.w);const use=rows.slice(0,6),sw=use.reduce((s,r)=>s+r.w,0);return{ok:true,deltaLog:use.reduce((s,r)=>s+r.w*r.deltaLog,0)/(sw+EPS),deltaTurn:use.reduce((s,r)=>s+r.w*r.deltaTurn,0)/(sw+EPS),support:use.length,weight:sw,pairDist:mean(use.map(r=>r.pairDist))}}
function transformPath(active,ctx,anchor,target,order){let cur=[...anchor.action],predLog=anchor.logGain,predTurn=anchor.observedTurn,support=0,weight=1,pairDist=[];for(const dim of order){if(cur[dim]===target[dim])continue;const e=stepEffect(active,ctx,dim,cur[dim],target[dim],cur[1-dim]);if(!e.ok)return{ok:false,predLog:null,support,pairDist:pairDist.length?mean(pairDist):null};predLog+=e.deltaLog;predTurn=wrap(predTurn+e.deltaTurn);support+=e.support;weight*=Math.max(EPS,e.weight);if(e.pairDist!=null)pairDist.push(e.pairDist);cur[dim]=target[dim]}return{ok:true,predLog,predTurn,support,weight,pairDist:pairDist.length?mean(pairDist):0}}
function matchedTransition(active,ctx){const anchors=[...active].sort((a,b)=>a.d-b.d).slice(0,MAX_ANCHORS),candidates=[];for(const target of ACTIONS){const preds=[];for(const anchor of anchors){const p01=transformPath(active,ctx,anchor,target,[0,1]),p10=transformPath(active,ctx,anchor,target,[1,0]),paths=[p01,p10].filter(p=>p.ok);if(!paths.length)continue;const predLog=mean(paths.map(p=>p.predLog)),agreement=paths.length===2?Math.abs(p01.predLog-p10.predLog):null,support=paths.reduce((s,p)=>s+p.support,0),pairDist=mean(paths.map(p=>p.pairDist).filter(x=>x!=null)),confidence=anchor.w*(1+Math.min(12,support)/12)*(agreement==null?.72:Math.exp(-agreement/.035));preds.push({predLog,support,pairDist,confidence,agreement})}if(!preds.length){candidates.push({action:target,ok:false,predLog:Infinity,support:0,anchorSupport:0});continue}const sw=preds.reduce((s,p)=>s+p.confidence,0),predLog=preds.reduce((s,p)=>s+p.confidence*p.predLog,0)/(sw+EPS);candidates.push({action:target,ok:true,predLog,support:Math.round(mean(preds.map(p=>p.support))),anchorSupport:preds.length,pathAgreement:mean(preds.map(p=>p.agreement).filter(x=>x!=null)),pairDist:mean(preds.map(p=>p.pairDist).filter(x=>x!=null))})}const valid=candidates.filter(c=>c.ok&&c.anchorSupport>=3).sort((a,b)=>a.predLog-b.predLog),chosen=valid[0]||{action:[0,0],support:0,anchorSupport:0,pathAgreement:null,pairDist:null};return{action:chosen.action,diag:{candidateCoverage:valid.length/9,support:chosen.support,anchorSupport:chosen.anchorSupport,pathAgreement:chosen.pathAgreement,pairDist:chosen.pairDist}}}
function oracle(ctx,seed,i){let a=ACTIONS[0],d=null,z=-Infinity;for(const x of ACTIONS){const q=dynamics(ctx,x,`q:${seed}:${i}:${key(x)}`);if(q.resolution>z){z=q.resolution;a=x;d=q}}return{action:a,dyn:d,resolution:z}}
function evalOne(active,ctx,orc,seed,i){const mt=matchedTransition(active,ctx),acts={matchedTransition:mt.action,relationProcess:relationProcess(active,ctx),localRewardRidge:localRewardRidge(active,ctx)},out={};for(const[n,a]of Object.entries(acts)){const d=dynamics(ctx,a,`q:${seed}:${i}:${key(a)}`);out[n]={action:a,stable:d.gain<1,oracle:key(a)===key(orc.action),regret:orc.resolution-d.resolution,localNovel:!active.some(r=>key(r.action)===key(a)),diag:n==='matchedTransition'?mt.diag:null}}return out}
const memory=makeMemory(),trials=[];
for(const seed of HOLDOUT_SEEDS){let accepted=0,attempt=0;while(accepted<QUERIES_PER_SEED&&attempt<QUERIES_PER_SEED*120){const i=attempt++,ctx=context(seed,i,'query'),orc=oracle(ctx,seed,i);if(ctx.baseGain<1.025||orc.dyn.gain>=.995)continue;const maxActive=activate(memory,ctx,2),ok=key(orc.action),oracleRows=maxActive.filter(r=>key(r.action)===ok);if(!oracleRows.length)continue;const maxLoco=maxActive.filter(r=>key(r.action)!==ok),c0=maxLoco.filter(r=>r.action[0]===orc.action[0]).length,c1=maxLoco.filter(r=>r.action[1]===orc.action[1]).length;if(maxLoco.length<30||c0<3||c1<3)continue;const byScale={};for(const scale of SCALES){const active=activate(memory,ctx,scale),loco=active.filter(r=>key(r.action)!==ok);byScale[String(scale)]={activeN:active.length,locoN:loco.length,removed:active.length-loco.length,componentSupport:[loco.filter(r=>r.action[0]===orc.action[0]).length,loco.filter(r=>r.action[1]===orc.action[1]).length],result:evalOne(loco,ctx,orc,seed,i)}}trials.push({seed,i,flow:ctx.commonFlowLoad,oracleAction:orc.action,byScale});accepted++}}
const MODELS=['matchedTransition','relationProcess','localRewardRidge'];
function summarize(scale,name){const xs=trials.map(t=>t.byScale[String(scale)].result[name]),ds=xs.map(x=>x.diag).filter(Boolean);return{n:xs.length,stableRate:mean(xs.map(x=>x.stable?1:0)),oracleRate:mean(xs.map(x=>x.oracle?1:0)),meanRegret:mean(xs.map(x=>x.regret)),novelRate:mean(xs.map(x=>x.localNovel?1:0)),diagnostics:ds.length?{support:mean(ds.map(d=>d.support)),coverage:mean(ds.map(d=>d.candidateCoverage)),anchorSupport:mean(ds.map(d=>d.anchorSupport)),pathAgreement:mean(ds.map(d=>d.pathAgreement).filter(x=>x!=null)),pairDist:mean(ds.map(d=>d.pairDist).filter(x=>x!=null))}:null,meanActiveN:mean(trials.map(t=>t.byScale[String(scale)].activeN)),meanLocoN:mean(trials.map(t=>t.byScale[String(scale)].locoN))}}
const summary={};for(const scale of SCALES){summary[String(scale)]={};for(const name of MODELS)summary[String(scale)][name]=summarize(scale,name)}
const report={design:{koreanQuestion:'동일 matched relational transition 메커니즘과 동일 query에서 모든 모델의 중립 활성 경험예산만 0.5배/1배/2배로 바꿀 때, 관계변환 support 증가가 LOCO 안정화 증가와 함께 나타나는가? 동시에 더 먼 matched pair를 끌어오는 bias 징후는 있는가?',englishTermNote:'Common support/overlap = 비교하려는 행동 조건들이 비슷한 현재 맥락에서 모두 관측되는 데이터 지지영역. Budget scaling = 모델별 특혜 없이 동일 neutral flow budget을 배수로 조절하는 진단.',scope:'신규성/우월성 실험이 아니다. 이전 holdout seed를 재사용하지 않는다. 같은 query를 세 budget에 paired evaluation하며 exact oracle composition은 각 budget의 active memory에서 제거한다.',nonCircularity:'새 holdout seeds, budget scales, acceptance rule, matchedTransition formula와 support/caliper 규칙은 첫 실행 전에 고정한다. 성능 방향을 CI 성공조건으로 사용하지 않는다.',trainSeeds:TRAIN_SEEDS,holdoutSeeds:HOLDOUT_SEEDS,scales:SCALES,memoryEpisodes:memory.length,models:MODELS},counts:{trials:trials.length},summary,trials};
console.log('\nOASIS RELATION SUPPORT-SCALING HOLDOUT');console.log(`trials=${trials.length} memory=${memory.length}`);for(const scale of SCALES){console.log(`SCALE ${scale}`);for(const name of MODELS){const r=summary[String(scale)][name],d=r.diagnostics;console.log(`${name}: stable=${r.stableRate.toFixed(3)} oracle=${r.oracleRate.toFixed(3)} regret=${r.meanRegret.toFixed(4)} novel=${r.novelRate.toFixed(3)} active=${r.meanActiveN.toFixed(1)}${d?` support=${d.support.toFixed(2)} pairDist=${d.pairDist.toFixed(3)} coverage=${d.coverage.toFixed(3)}`:''}`)}}console.log('RESULT: paired support-scaling holdout completed without a model-winning assertion.');
await writeFile('relation-support-scaling-holdout-report.json',JSON.stringify(report,null,2));
