# Founding Flow v3 restart marker

This commit restarts the already frozen Founding Flow v3 pipeline after the prior audit false positive.

No experimental world condition, archetype principle, seed, round count, evaluation rule, or OASIS core behavior is changed by this marker.
